# Explicitly use the deterministic fixture provider for local development.
EXTRACTION_PROVIDER=mock

# Required only when EXTRACTION_PROVIDER=openai.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
OPENAI_TIMEOUT_MS=4500

PORT=3000
MAX_FILE_SIZE_BYTES=10485760
MAX_IMAGE_PIXELS=25000000
